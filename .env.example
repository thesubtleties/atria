# =============================================================================
# DOCKER COMPOSE ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env for Docker Compose variable substitution
# This file is used by Docker Compose to substitute ${VARIABLES} in YAML files
# It is NOT loaded into containers - use .env.development for that
#
# IMPORTANT: Keep these values in sync with .env.development
# The PostgreSQL container uses these to initialize the database,
# and the backend must use matching credentials to connect.

# =============================================================================
# PostgreSQL Database Initialization
# =============================================================================
# These credentials are used to CREATE the PostgreSQL database
POSTGRES_USER=dev_user
POSTGRES_PASSWORD=dev_password
POSTGRES_DB=atria_dev

# =============================================================================
# Database Seeding (Optional)
# =============================================================================
# Automatically seed the database with sample data on startup
# Default: true (if not set, docker-compose defaults to true)
SEED_DB=true
