# Database
POSTGRES_USER=example_user
POSTGRES_PASSWORD=example_password
POSTGRES_DB=example_db
POSTGRES_PORT=5432

# Backend
BACKEND_PORT=5000

# Frontend
FRONTEND_PORT=5173
VITE_API_URL=http://localhost:5000/api

# Development flags (optional)
# VITE_FORCE_HTTP_FALLBACK=true  # Force HTTP instead of WebSocket (testing only)